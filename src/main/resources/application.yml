spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/mobang?autoReconnection=true
    username: mobang
    password: mobang
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl_auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  port: 8000